// AniList forecast page removed.
// This placeholder file remains to avoid import errors in older branches.
// The UI entry point for AniList forecasts was removed from the Watchlist.
// If you want to fully delete this file, ensure no references remain elsewhere.

import 'package:flutter/widgets.dart';

/// Deprecated placeholder
class AniListForecastPage extends StatelessWidget {
  const AniListForecastPage({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
