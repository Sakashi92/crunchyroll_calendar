{
    "sourceFile": "lib/services/background_service.dart",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 34,
            "patches": [
                {
                    "date": 1768745838948,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1768746199681,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -82,10 +82,12 @@\n   \r\n   /// Prüft ob eine Aufgabe bereits läuft\r\n   Future<bool> isTaskRunning() async {\r\n     try {\r\n-      final allTasks = await Workmanager().getAllTasks();\r\n-      return allTasks.any((task) => task.task == _taskName);\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n     } catch (e) {\r\n       if (kDebugMode) print('❌ Error checking task status: $e');\r\n       return false;\r\n     }\r\n"
                },
                {
                    "date": 1768750074637,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,228 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in favorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768750112660,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,284 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.not_required,\r\n+        ),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in favorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768750176430,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,281 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in favorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768834886997,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,288 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768835991974,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,362 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    if (!kDebugMode) {\r\n+      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n+      return;\r\n+    }\r\n+\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768836041091,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,362 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    if (!kDebugMode) {\r\n+      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n+      return;\r\n+    }\r\n+\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768836599258,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,369 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    if (!kDebugMode) {\r\n+      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n+      return;\r\n+    }\r\n+\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768837683095,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,385 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow; // Re-throw so we see the error in main\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    if (!kDebugMode) {\r\n+      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n+      return;\r\n+    }\r\n+\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (taskName == BackgroundService._taskName) {\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Background task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768837699767,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,392 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow; // Re-throw so we see the error in main\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    if (!kDebugMode) {\r\n+      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n+      return;\r\n+    }\r\n+\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768837898540,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,421 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    if (!kDebugMode) {\r\n+      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n+      return;\r\n+    }\r\n+\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768838764867,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,416 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768839361112,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,418 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Network Constraint für schnelle Tests!\r\n+        constraints: Constraints(networkType: NetworkType.not_required),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768839415388,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,417 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  No favorites configured');\r\n+      return true;\r\n+    }\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768839905862,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,422 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  No releases found');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768839915782,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,425 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        if (kDebugMode) {\r\n+          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n+          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n+          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n+          final favoriteNames = \r\n+              enabledFavorites.map((f) => f.title).toList();\r\n+          await notificationService\r\n+              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+          if (kDebugMode) {\r\n+            print('✓ Test-Benachrichtigungen gesendet');\r\n+          }\r\n+        }\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      if (kDebugMode) {\r\n+        print('✓ Test-Benachrichtigungen gesendet für '\r\n+            '${favoritesWithTodaysReleases.length} Favoriten');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768840138327,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,421 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      print('📚 [TEST] Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      print('🔔 [TEST] Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        print('ℹ️  [TEST] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      print('🌐 [TEST] Fetching releases for this week...');\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      print('📺 [TEST] Found ${todaysReleases.length} releases for this week');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        print('ℹ️  [TEST] Keine Favoriten mit Releases von heute');\r\n+        print('💡 [TEST] Sende Test-Benachrichtigungen für alle ${enabledFavorites.length} enabled Favoriten...');\r\n+        final favoriteNames = \r\n+            enabledFavorites.map((f) => f.title).toList();\r\n+        print('📤 [TEST] Sending notifications for: ${favoriteNames.join(\", \")}');\r\n+        await notificationService\r\n+            .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+        print('✅ [TEST] Test-Benachrichtigungen gesendet');\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      print('📤 [TEST] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      print('📤 [TEST] Favorite names: ${favoriteNames.join(\", \")}');\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      print('✅ [TEST] Test-Benachrichtigungen gesendet für '\r\n+          '${favoritesWithTodaysReleases.length} Favoriten');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error sending test notifications: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768840786877,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,422 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Nur im Debug-Mode verwendbar\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Starting test notifications for today\\'s favorites...');\r\n+      \r\n+      final favoritesRepo = FavoritesRepository();\r\n+      final crunchyrollService = CrunchyrollService();\r\n+      final notificationService = NotificationService();\r\n+\r\n+      // Lade alle Favoriten\r\n+      final allFavorites = await favoritesRepo.getAllFavorites();\r\n+      print('📚 [TEST] Loaded ${allFavorites.length} favorites');\r\n+\r\n+      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+      final enabledFavorites = \r\n+          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+      print('🔔 [TEST] Enabled favorites: ${enabledFavorites.length}');\r\n+\r\n+      if (enabledFavorites.isEmpty) {\r\n+        print('ℹ️  [TEST] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+        return;\r\n+      }\r\n+\r\n+      // Lade heutige Releases\r\n+      print('🌐 [TEST] Fetching releases for this week...');\r\n+      final today = DateTime.now();\r\n+      final todaysReleases = \r\n+          await crunchyrollService.getReleasesForWeek(today);\r\n+      print('📺 [TEST] Found ${todaysReleases.length} releases for this week');\r\n+\r\n+      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+        return todaysReleases.any((release) =>\r\n+            release.title.toLowerCase() == fav.title.toLowerCase());\r\n+      }).toList();\r\n+\r\n+      if (favoritesWithTodaysReleases.isEmpty) {\r\n+        print('ℹ️  [TEST] Keine Favoriten mit Releases von heute');\r\n+        print('💡 [TEST] Sende Test-Benachrichtigungen für alle ${enabledFavorites.length} enabled Favoriten...');\r\n+        final favoriteNames = \r\n+            enabledFavorites.map((f) => f.title).toList();\r\n+        print('📤 [TEST] Sending notifications for: ${favoriteNames.join(\", \")}');\r\n+        await notificationService\r\n+            .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+        print('✅ [TEST] Test-Benachrichtigungen gesendet');\r\n+        return;\r\n+      }\r\n+\r\n+      // Sende Test-Benachrichtigungen\r\n+      print('📤 [TEST] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+      final favoriteNames = \r\n+          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n+      print('📤 [TEST] Favorite names: ${favoriteNames.join(\", \")}');\r\n+      await notificationService\r\n+          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n+\r\n+      print('✅ [TEST] Test-Benachrichtigungen gesendet für '\r\n+          '${favoritesWithTodaysReleases.length} Favoriten');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error sending test notifications: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768840804471,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,381 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768840831275,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,384 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768840851836,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -358,9 +358,9 @@\n \r\n /// Test-Benachrichtigung ausführen (im Background Isolate)\r\n Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n   try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n     \r\n     final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n     \r\n     // Initialisiere NotificationService\r\n@@ -373,6934 +373,93 @@\n       body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n       payload: 'background_test',\r\n     );\r\n     \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n     \r\n     return true;\r\n   } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n     return false;\r\n   }\r\n }\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n \r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n   try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n     \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    // Initialisiere Services\r\n     final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n     final crunchyrollService = CrunchyrollService();\r\n     final notificationService = NotificationService();\r\n     \r\n     // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n       return false;\r\n     }\r\n     \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n     \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n     \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n     if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n       return true;\r\n     }\r\n     \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    // Lade heutige Releases\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final today = DateTime.now();\r\n+    final todaysReleases = \r\n+        await crunchyrollService.getReleasesForWeek(today);\r\n+    print('📺 [BACKGROUND] Found ${todaysReleases.length} releases for this week');\r\n     \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    // Finde Schnittmenge: Favoriten mit Releases von heute\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n     \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n+    List<String> favoriteNames;\r\n     \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n-      return true;\r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      print('💡 [BACKGROUND] Sende Test-Benachrichtigungen für alle ${enabledFavorites.length} enabled Favoriten...');\r\n+      favoriteNames = enabledFavorites.map((f) => f.title).toList();\r\n+    } else {\r\n+      print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+      favoriteNames = favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n     }\r\n     \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n+    // Sende Benachrichtigungen für jeden Favoriten\r\n+    for (int i = 0; i < favoriteNames.length; i++) {\r\n+      final title = favoriteNames[i];\r\n+      print('📤 [BACKGROUND] Sending notification ${i+1}/${favoriteNames.length} for: $title');\r\n       \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n       await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n+        title: '🔔 Neue Episode: $title',\r\n+        body: 'Eine neue Episode von $title ist heute verfügbar!',\r\n+        payload: 'favorite_$i',\r\n       );\r\n       \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+      // Kleine Verzögerung zwischen Benachrichtigungen\r\n+      await Future.delayed(const Duration(milliseconds: 500));\r\n     }\r\n     \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n+    print('✅ [BACKGROUND] All ${favoriteNames.length} notifications sent!');\r\n     return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n     return false;\r\n   }\r\n }\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      print('📚 [TEST] Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      print('🔔 [TEST] Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        print('ℹ️  [TEST] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      print('🌐 [TEST] Fetching releases for this week...');\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      print('📺 [TEST] Found ${todaysReleases.length} releases for this week');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        print('ℹ️  [TEST] Keine Favoriten mit Releases von heute');\r\n-        print('💡 [TEST] Sende Test-Benachrichtigungen für alle ${enabledFavorites.length} enabled Favoriten...');\r\n-        final favoriteNames = \r\n-            enabledFavorites.map((f) => f.title).toList();\r\n-        print('📤 [TEST] Sending notifications for: ${favoriteNames.join(\", \")}');\r\n-        await notificationService\r\n-            .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-        print('✅ [TEST] Test-Benachrichtigungen gesendet');\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      print('📤 [TEST] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      print('📤 [TEST] Favorite names: ${favoriteNames.join(\", \")}');\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      print('✅ [TEST] Test-Benachrichtigungen gesendet für '\r\n-          '${favoritesWithTodaysReleases.length} Favoriten');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error sending test notifications: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      print('📚 [TEST] Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      print('🔔 [TEST] Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        print('ℹ️  [TEST] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      print('🌐 [TEST] Fetching releases for this week...');\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      print('📺 [TEST] Found ${todaysReleases.length} releases for this week');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        print('ℹ️  [TEST] Keine Favoriten mit Releases von heute');\r\n-        print('💡 [TEST] Sende Test-Benachrichtigungen für alle ${enabledFavorites.length} enabled Favoriten...');\r\n-        final favoriteNames = \r\n-            enabledFavorites.map((f) => f.title).toList();\r\n-        print('📤 [TEST] Sending notifications for: ${favoriteNames.join(\", \")}');\r\n-        await notificationService\r\n-            .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-        print('✅ [TEST] Test-Benachrichtigungen gesendet');\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      print('📤 [TEST] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      print('📤 [TEST] Favorite names: ${favoriteNames.join(\", \")}');\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      print('✅ [TEST] Test-Benachrichtigungen gesendet für '\r\n-          '${favoritesWithTodaysReleases.length} Favoriten');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error sending test notifications: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Network Constraint für schnelle Tests!\r\n-        constraints: Constraints(networkType: NetworkType.not_required),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    if (!kDebugMode) {\r\n-      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n-      return;\r\n-    }\r\n-\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow; // Re-throw so we see the error in main\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    if (!kDebugMode) {\r\n-      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n-      return;\r\n-    }\r\n-\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow; // Re-throw so we see the error in main\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    if (!kDebugMode) {\r\n-      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n-      return;\r\n-    }\r\n-\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    if (!kDebugMode) {\r\n-      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n-      return;\r\n-    }\r\n-\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('⚠️  Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    if (!kDebugMode) {\r\n-      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n-      return;\r\n-    }\r\n-\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getAllFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Nur im Debug-Mode verwendbar\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    if (!kDebugMode) {\r\n-      if (kDebugMode) print('⚠️  Test notifications only available in debug mode');\r\n-      return;\r\n-    }\r\n-\r\n-    try {\r\n-      if (kDebugMode) print('🔔 Starting test notifications for today\\'s favorites...');\r\n-      \r\n-      final favoritesRepo = FavoritesRepository();\r\n-      final crunchyrollService = CrunchyrollService();\r\n-      final notificationService = NotificationService();\r\n-\r\n-      // Lade alle Favoriten\r\n-      final allFavorites = await favoritesRepo.getFavorites();\r\n-      if (kDebugMode) print('📚 Loaded ${allFavorites.length} favorites');\r\n-\r\n-      // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-      final enabledFavorites = \r\n-          allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-      if (kDebugMode) print('🔔 Enabled favorites: ${enabledFavorites.length}');\r\n-\r\n-      if (enabledFavorites.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Lade heutige Releases\r\n-      final today = DateTime.now();\r\n-      final todaysReleases = \r\n-          await crunchyrollService.getReleasesForWeek(today);\r\n-      if (kDebugMode) print('📺 Found ${todaysReleases.length} releases for today');\r\n-\r\n-      // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-      final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-        return todaysReleases.any((release) =>\r\n-            release.title.toLowerCase() == fav.title.toLowerCase());\r\n-      }).toList();\r\n-\r\n-      if (favoritesWithTodaysReleases.isEmpty) {\r\n-        if (kDebugMode) {\r\n-          print('ℹ️  Keine Favoriten mit Releases von heute');\r\n-          // Fallback: Sende Test-Benachrichtigungen für alle enabled Favoriten\r\n-          print('💡 Sende Test-Benachrichtigungen für alle enabled Favoriten...');\r\n-          final favoriteNames = \r\n-              enabledFavorites.map((f) => f.title).toList();\r\n-          await notificationService\r\n-              .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-          if (kDebugMode) {\r\n-            print('✓ Test-Benachrichtigungen gesendet');\r\n-          }\r\n-        }\r\n-        return;\r\n-      }\r\n-\r\n-      // Sende Test-Benachrichtigungen\r\n-      final favoriteNames = \r\n-          favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-      await notificationService\r\n-          .sendTestNotificationForFavoritesOfToday(favoriteNames);\r\n-\r\n-      if (kDebugMode) {\r\n-        print('✓ Test-Benachrichtigungen gesendet für '\r\n-            '${favoritesWithTodaysReleases.length} Favoriten');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error sending test notifications: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in favorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.not_required,\r\n-        ),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        return await _executeTestNotification(inputData);\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in favorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    if (kDebugMode) print('🔔 Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    if (kDebugMode) print('✓ Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in favorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: kDebugMode,\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (taskName == BackgroundService._taskName) {\r\n-        return await _executeBackgroundScraper();\r\n-      }\r\n-      \r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Background task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  No releases found');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in favorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n"
                },
                {
                    "date": 1768841181799,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,483 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n+    \r\n+    if (currentReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: currentReleases,\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768841197133,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,500 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // 3. Scrape aktuelle Releases\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites today');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768841582583,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,500 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n+      print('  - Favorites checked: ${favorites.length}');\r\n+      print('  - Releases found: ${uniqueReleases.length}');\r\n+      print('  - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768841589113,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,499 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+    print('   - Favorites checked: ${favorites.length}');\r\n+    print('   - Releases found: ${uniqueReleases.length}');\r\n+    print('   - Notifications sent: $notificationCount');\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+    print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768841603781,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,520 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling favorites test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n+  Future<void> testBackgroundScraperNow() async {\r\n+    try {\r\n+      print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      \r\n+      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _taskName,  // Gleicher Task wie der periodische Scraper\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+      print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n+    } catch (e, stackTrace) {\r\n+      print('❌ [TEST] Error scheduling scraper test: $e');\r\n+      print('❌ [TEST] Stack trace: $stackTrace');\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+    print('   - Favorites checked: ${favorites.length}');\r\n+    print('   - Releases found: ${uniqueReleases.length}');\r\n+    print('   - Notifications sent: $notificationCount');\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+    print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768842073339,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -45,9 +45,9 @@\n   /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n   /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n   Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n     try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n       \r\n       final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n       \r\n       await Workmanager().registerOneOffTask(\r\n@@ -56,20 +56,24 @@\n         // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n         initialDelay: const Duration(seconds: 3),\r\n       );\r\n       \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      }\r\n     } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling favorites test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n     }\r\n   }\r\n \r\n   /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n   Future<void> testBackgroundScraperNow() async {\r\n     try {\r\n-      print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n       \r\n       final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n       \r\n       await Workmanager().registerOneOffTask(\r\n@@ -77,515 +81,20 @@\n         _taskName,  // Gleicher Task wie der periodische Scraper\r\n         initialDelay: const Duration(seconds: 3),\r\n       );\r\n       \r\n-      print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling scraper test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n       if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n+        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n       }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n+    } catch (e, stackTrace) {\r\n       if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n+        print('❌ [TEST] Error scheduling scraper test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n       }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n     }\r\n   }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n \r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n-    print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    \r\n-    print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n-    \r\n-    if (allReleases.isEmpty) {\r\n-      print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 3. Hole alle Favoriten\r\n-    print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: todaysReleases,  // Nur heutige Releases!\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n-    print('   - Favorites checked: ${favorites.length}');\r\n-    print('   - Releases found: ${uniqueReleases.length}');\r\n-    print('   - Notifications sent: $notificationCount');\r\n-    \r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n-    print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade Releases für diese Woche\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Finde Favoriten mit Releases von HEUTE\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n-    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n-      final favorite = favoritesWithTodaysReleases[i];\r\n-      \r\n-      // Finde die passenden Releases für diesen Favoriten\r\n-      final matchingReleases = todaysReleases.where((release) =>\r\n-          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n-      \r\n-      for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n-        \r\n-        await notificationService.showNotification(\r\n-          title: '🔔 Neue Episode: ${release.title}',\r\n-          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n-          payload: 'release_${release.title}_${release.episodeNumber}',\r\n-        );\r\n-        \r\n-        // Kleine Verzögerung zwischen Benachrichtigungen\r\n-        await Future.delayed(const Duration(milliseconds: 500));\r\n-      }\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n   /// Startet die periodische Scraper-Aufgabe\r\n   /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n   Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n     try {\r\n@@ -706,35 +215,35 @@\n /// Callback-Dispatcher für Background Tasks\r\n /// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n @pragma('vm:entry-point')\r\n void callbackDispatcher() {\r\n-  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n   \r\n   Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n     try {\r\n-      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n       \r\n       // Sofort-Test Task\r\n       if (taskName == 'workmanager_test_now') {\r\n-        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n         return Future.value(true);\r\n       }\r\n       \r\n       if (taskName == BackgroundService._taskName) {\r\n-        print('📱 [BACKGROUND] Running scraper task...');\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n         return await _executeBackgroundScraper();\r\n       } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        print('🧪 [BACKGROUND] Running test notification task...');\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n         return await _executeTestNotification(inputData);\r\n       } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n         return await _executeFavoritesTestNotification();\r\n       }\r\n       \r\n-      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n       return Future.value(true);\r\n     } catch (e) {\r\n-      print('❌ [BACKGROUND] Task error: $e');\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n       return Future.value(false);\r\n     }\r\n   });\r\n }\r\n@@ -1016,1952 +525,4 @@\n     print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n     return false;\r\n   }\r\n }\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n-    print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    \r\n-    print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n-    \r\n-    if (allReleases.isEmpty) {\r\n-      print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 3. Hole alle Favoriten\r\n-    print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: todaysReleases,  // Nur heutige Releases!\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade Releases für diese Woche\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Finde Favoriten mit Releases von HEUTE\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n-    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n-      final favorite = favoritesWithTodaysReleases[i];\r\n-      \r\n-      // Finde die passenden Releases für diesen Favoriten\r\n-      final matchingReleases = todaysReleases.where((release) =>\r\n-          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n-      \r\n-      for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n-        \r\n-        await notificationService.showNotification(\r\n-          title: '🔔 Neue Episode: ${release.title}',\r\n-          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n-          payload: 'release_${release.title}_${release.episodeNumber}',\r\n-        );\r\n-        \r\n-        // Kleine Verzögerung zwischen Benachrichtigungen\r\n-        await Future.delayed(const Duration(milliseconds: 500));\r\n-      }\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    \r\n-    if (allReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    if (kDebugMode) print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: todaysReleases,  // Nur heutige Releases!\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites today');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites today');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade Releases für diese Woche\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Finde Favoriten mit Releases von HEUTE\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n-    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n-      final favorite = favoritesWithTodaysReleases[i];\r\n-      \r\n-      // Finde die passenden Releases für diesen Favoriten\r\n-      final matchingReleases = todaysReleases.where((release) =>\r\n-          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n-      \r\n-      for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n-        \r\n-        await notificationService.showNotification(\r\n-          title: '🔔 Neue Episode: ${release.title}',\r\n-          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n-          payload: 'release_${release.title}_${release.episodeNumber}',\r\n-        );\r\n-        \r\n-        // Kleine Verzögerung zwischen Benachrichtigungen\r\n-        await Future.delayed(const Duration(milliseconds: 500));\r\n-      }\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade Releases für diese Woche\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Finde Favoriten mit Releases von HEUTE\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n-    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n-      final favorite = favoritesWithTodaysReleases[i];\r\n-      \r\n-      // Finde die passenden Releases für diesen Favoriten\r\n-      final matchingReleases = todaysReleases.where((release) =>\r\n-          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n-      \r\n-      for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n-        \r\n-        await notificationService.showNotification(\r\n-          title: '🔔 Neue Episode: ${release.title}',\r\n-          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n-          payload: 'release_${release.title}_${release.episodeNumber}',\r\n-        );\r\n-        \r\n-        // Kleine Verzögerung zwischen Benachrichtigungen\r\n-        await Future.delayed(const Duration(milliseconds: 500));\r\n-      }\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-      print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-    } catch (e, stackTrace) {\r\n-      print('❌ [TEST] Error scheduling favorites test: $e');\r\n-      print('❌ [TEST] Stack trace: $stackTrace');\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND] Background scraper started');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND] No favorites configured');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND] All favorites muted, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // 3. Scrape aktuelle Releases\r\n-    if (kDebugMode) print('🌐 [BACKGROUND] Fetching current releases from Crunchyroll...');\r\n-    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n-    \r\n-    if (currentReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND] Found ${currentReleases.length} current releases');\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: currentReleases,\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND] No new releases for favorites');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND] Found ${relevantReleases.length} releases for favorites');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    if (kDebugMode) {\r\n-      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n-      print('  - Favorites checked: ${favorites.length}');\r\n-      print('  - Releases found: ${uniqueReleases.length}');\r\n-      print('  - Notifications sent: $notificationCount');\r\n-    }\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade heutige Releases\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final today = DateTime.now();\r\n-    final todaysReleases = \r\n-        await crunchyrollService.getReleasesForWeek(today);\r\n-    print('📺 [BACKGROUND] Found ${todaysReleases.length} releases for this week');\r\n-    \r\n-    // Finde Schnittmenge: Favoriten mit Releases von heute\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    List<String> favoriteNames;\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      print('💡 [BACKGROUND] Sende Test-Benachrichtigungen für alle ${enabledFavorites.length} enabled Favoriten...');\r\n-      favoriteNames = enabledFavorites.map((f) => f.title).toList();\r\n-    } else {\r\n-      print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-      favoriteNames = favoritesWithTodaysReleases.map((f) => f.title).toList();\r\n-    }\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten\r\n-    for (int i = 0; i < favoriteNames.length; i++) {\r\n-      final title = favoriteNames[i];\r\n-      print('📤 [BACKGROUND] Sending notification ${i+1}/${favoriteNames.length} for: $title');\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: '🔔 Neue Episode: $title',\r\n-        body: 'Eine neue Episode von $title ist heute verfügbar!',\r\n-        payload: 'favorite_$i',\r\n-      );\r\n-      \r\n-      // Kleine Verzögerung zwischen Benachrichtigungen\r\n-      await Future.delayed(const Duration(milliseconds: 500));\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All ${favoriteNames.length} notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n"
                },
                {
                    "date": 1768842090586,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,528 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling favorites test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n+  Future<void> testBackgroundScraperNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      \r\n+      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _taskName,  // Gleicher Task wie der periodische Scraper\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling scraper test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+    print('   - Favorites checked: ${favorites.length}');\r\n+    print('   - Releases found: ${uniqueReleases.length}');\r\n+    print('   - Notifications sent: $notificationCount');\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+    print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768842109228,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -319,28 +319,28 @@\n       return true;\r\n     }\r\n     \r\n     // 4. Filtere nach Favoriten\r\n-    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    if (kDebugMode) print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n     final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n     final relevantReleases = ReleaseComparator.filterByFavorites(\r\n       releases: todaysReleases,  // Nur heutige Releases!\r\n       favorites: favoritesTitles,\r\n     );\r\n     \r\n     if (relevantReleases.isEmpty) {\r\n-      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      if (kDebugMode) print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n       return true;\r\n     }\r\n     \r\n-    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n     \r\n     // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n     final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n     \r\n     // 6. Sende Benachrichtigungen für neue Releases\r\n     int notificationCount = 0;\r\n-    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    if (kDebugMode) print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n     \r\n     for (final release in uniqueReleases) {\r\n       // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n       final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n@@ -349,9 +349,9 @@\n         release.episodeNumber,\r\n       );\r\n       \r\n       if (alreadyNotified) {\r\n-        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        if (kDebugMode) print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n         continue;\r\n       }\r\n       \r\n       // Zeige Benachrichtigung\r\n@@ -525,532 +525,4 @@\n     print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n     return false;\r\n   }\r\n }\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-      }\r\n-    } catch (e, stackTrace) {\r\n-      if (kDebugMode) {\r\n-        print('❌ [TEST] Error scheduling favorites test: $e');\r\n-        print('❌ [TEST] Stack trace: $stackTrace');\r\n-      }\r\n-    }\r\n-  }\r\n-\r\n-  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n-  Future<void> testBackgroundScraperNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n-      \r\n-      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _taskName,  // Gleicher Task wie der periodische Scraper\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n-        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n-      }\r\n-    } catch (e, stackTrace) {\r\n-      if (kDebugMode) {\r\n-        print('❌ [TEST] Error scheduling scraper test: $e');\r\n-        print('❌ [TEST] Stack trace: $stackTrace');\r\n-      }\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n-    print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    \r\n-    print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n-    \r\n-    if (allReleases.isEmpty) {\r\n-      print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 3. Hole alle Favoriten\r\n-    print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: todaysReleases,  // Nur heutige Releases!\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n-    print('   - Favorites checked: ${favorites.length}');\r\n-    print('   - Releases found: ${uniqueReleases.length}');\r\n-    print('   - Notifications sent: $notificationCount');\r\n-    \r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n-    print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade Releases für diese Woche\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Finde Favoriten mit Releases von HEUTE\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n-    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n-      final favorite = favoritesWithTodaysReleases[i];\r\n-      \r\n-      // Finde die passenden Releases für diesen Favoriten\r\n-      final matchingReleases = todaysReleases.where((release) =>\r\n-          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n-      \r\n-      for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n-        \r\n-        await notificationService.showNotification(\r\n-          title: '🔔 Neue Episode: ${release.title}',\r\n-          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n-          payload: 'release_${release.title}_${release.episodeNumber}',\r\n-        );\r\n-        \r\n-        // Kleine Verzögerung zwischen Benachrichtigungen\r\n-        await Future.delayed(const Duration(milliseconds: 500));\r\n-      }\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n"
                },
                {
                    "date": 1768842244638,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,532 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling favorites test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n+  Future<void> testBackgroundScraperNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      \r\n+      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _taskName,  // Gleicher Task wie der periodische Scraper\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling scraper test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+      print('   - Favorites checked: ${favorites.length}');\r\n+      print('   - Releases found: ${uniqueReleases.length}');\r\n+      print('   - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+      print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768842401198,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,9 +30,9 @@\n       if (kDebugMode) print('🔧 Initializing Background Service...');\r\n       \r\n       await Workmanager().initialize(\r\n         callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n+        isInDebugMode: kDebugMode, // Nur im Debug-Modus Logs anzeigen\r\n       );\r\n       \r\n       if (kDebugMode) print('✓ Background Service initialized successfully');\r\n     } catch (e) {\r\n@@ -508,9 +508,9 @@\n       final matchingReleases = todaysReleases.where((release) =>\r\n           release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n       \r\n       for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        if (kDebugMode) print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n         \r\n         await notificationService.showNotification(\r\n           title: '🔔 Neue Episode: ${release.title}',\r\n           body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n@@ -521,540 +521,14 @@\n         await Future.delayed(const Duration(milliseconds: 500));\r\n       }\r\n     }\r\n     \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n+    if (kDebugMode) print('✅ [BACKGROUND] All notifications sent!');\r\n     return true;\r\n   } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n-import 'package:workmanager/workmanager.dart';\r\n-import 'package:flutter/foundation.dart';\r\n-import '../repositories/favorites_repository.dart';\r\n-import '../repositories/notification_repository.dart';\r\n-import '../services/crunchyroll_service.dart';\r\n-import '../services/notification_service.dart';\r\n-import '../utils/release_comparator.dart';\r\n-import '../models/notification_log.dart';\r\n-\r\n-/// Service für Background-Scraping wenn die App geschlossen ist\r\n-/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n-class BackgroundService {\r\n-  static const String _taskName = 'crunchyrollScraperTask';\r\n-  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n-  static const String _testNotificationTaskName = 'testNotificationTask';\r\n-  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n-  \r\n-  static final BackgroundService _instance = BackgroundService._internal();\r\n-  \r\n-  factory BackgroundService() {\r\n-    return _instance;\r\n-  }\r\n-  \r\n-  BackgroundService._internal();\r\n-  \r\n-  /// Initialisiert den Background Service\r\n-  /// Muss beim App-Start aufgerufen werden\r\n-  static Future<void> initialize() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n-      \r\n-      await Workmanager().initialize(\r\n-        callbackDispatcher,\r\n-        isInDebugMode: true, // WICHTIG: Zeigt Workmanager Logs\r\n-      );\r\n-      \r\n-      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n-      rethrow;\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+      print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n     }\r\n-  }\r\n-  \r\n-  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n-  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n-  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n-  Future<void> sendTestNotificationsForTodaysFavorites() async {\r\n-    try {\r\n-      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager...');\r\n-      \r\n-      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _favoritesTestTaskName,\r\n-        // Kleine Verzögerung damit die App Zeit hat sich zu schließen\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ [TEST] Favorites test task scheduled - will run in ~3 seconds');\r\n-        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n-      }\r\n-    } catch (e, stackTrace) {\r\n-      if (kDebugMode) {\r\n-        print('❌ [TEST] Error scheduling favorites test: $e');\r\n-        print('❌ [TEST] Stack trace: $stackTrace');\r\n-      }\r\n-    }\r\n-  }\r\n-\r\n-  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n-  Future<void> testBackgroundScraperNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n-      \r\n-      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _taskName,  // Gleicher Task wie der periodische Scraper\r\n-        initialDelay: const Duration(seconds: 3),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n-        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n-      }\r\n-    } catch (e, stackTrace) {\r\n-      if (kDebugMode) {\r\n-        print('❌ [TEST] Error scheduling scraper test: $e');\r\n-        print('❌ [TEST] Stack trace: $stackTrace');\r\n-      }\r\n-    }\r\n-  }\r\n-\r\n-  /// Startet die periodische Scraper-Aufgabe\r\n-  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n-  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n-    try {\r\n-      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n-      \r\n-      if (intervalMinutes < 15) {\r\n-        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n-        intervalMinutes = 15;\r\n-      }\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('🔍 Registering task with:');\r\n-        print('   - Unique name: $_uniqueName');\r\n-        print('   - Task name: $_taskName');\r\n-        print('   - Interval: $intervalMinutes minutes');\r\n-        print('   - Network constraint: connected (REQUIRED)');\r\n-      }\r\n-      \r\n-      await Workmanager().registerPeriodicTask(\r\n-        _uniqueName,\r\n-        _taskName,\r\n-        frequency: Duration(minutes: intervalMinutes),\r\n-        constraints: Constraints(\r\n-          networkType: NetworkType.connected,\r\n-          requiresBatteryNotLow: false,\r\n-          requiresCharging: false,\r\n-          requiresDeviceIdle: false,\r\n-          requiresStorageNotLow: false,\r\n-        ),\r\n-        backoffPolicy: BackoffPolicy.exponential,\r\n-        backoffPolicyDelay: const Duration(minutes: 15),\r\n-        initialDelay: const Duration(minutes: 5),\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n-        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n-        print('   (If device has internet connection)');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n-      rethrow;\r\n-    }\r\n-  }\r\n-  \r\n-  /// Stoppt die periodische Scraper-Aufgabe\r\n-  Future<void> stopPeriodicScraperTask() async {\r\n-    try {\r\n-      await Workmanager().cancelByUniqueName(_uniqueName);\r\n-      \r\n-      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n-    }\r\n-  }\r\n-  \r\n-  /// Prüft ob eine Aufgabe bereits läuft\r\n-  Future<bool> isTaskRunning() async {\r\n-    try {\r\n-      // Workmanager hat keine getAllTasks() API\r\n-      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n-      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n-      return true; // Task läuft wenn sie registriert wurde\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error checking task status: $e');\r\n-      return false;\r\n-    }\r\n-  }\r\n-\r\n-  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n-  /// Funktioniert auch wenn die App geschlossen ist\r\n-  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n-    try {\r\n-      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        uniqueName,\r\n-        _testNotificationTaskName,\r\n-        initialDelay: Duration(seconds: delaySeconds),\r\n-        inputData: {\r\n-          'delay_seconds': delaySeconds,\r\n-          'scheduled_time': DateTime.now().toIso8601String(),\r\n-        },\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n-    }\r\n-  }\r\n-\r\n-  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n-  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n-  Future<void> testWorkmanagerNow() async {\r\n-    try {\r\n-      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n-      \r\n-      await Workmanager().registerOneOffTask(\r\n-        'workmanager_test_now',\r\n-        'workmanager_test_now',\r\n-        // Keine Verzögerung! Sollte sofort laufen\r\n-        // Keine Constraints für schnelle Tests\r\n-      );\r\n-      \r\n-      if (kDebugMode) {\r\n-        print('✅ Test task registered - check logs in 2-3 seconds');\r\n-        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n-      }\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ Error registering test task: $e');\r\n-    }\r\n-  }\r\n-}\r\n-\r\n-/// Callback-Dispatcher für Background Tasks\r\n-/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n-@pragma('vm:entry-point')\r\n-void callbackDispatcher() {\r\n-  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n-  \r\n-  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n-    try {\r\n-      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n-      \r\n-      // Sofort-Test Task\r\n-      if (taskName == 'workmanager_test_now') {\r\n-        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n-        return Future.value(true);\r\n-      }\r\n-      \r\n-      if (taskName == BackgroundService._taskName) {\r\n-        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n-        return await _executeBackgroundScraper();\r\n-      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n-        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n-        return await _executeTestNotification(inputData);\r\n-      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n-        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n-        return await _executeFavoritesTestNotification();\r\n-      }\r\n-      \r\n-      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n-      return Future.value(true);\r\n-    } catch (e) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n-      return Future.value(false);\r\n-    }\r\n-  });\r\n-}\r\n-\r\n-/// Hauptfunktion für Background-Scraping\r\n-/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n-Future<bool> _executeBackgroundScraper() async {\r\n-  try {\r\n-    if (kDebugMode) print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n-    \r\n-    final startTime = DateTime.now();\r\n-    \r\n-    // 1. Initialisiere Services\r\n-    if (kDebugMode) print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final notificationRepo = NotificationRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    if (kDebugMode) print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n-    final notificationInitialized = await notificationService.initialize();\r\n-    if (!notificationInitialized) {\r\n-      if (kDebugMode) print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n-    if (kDebugMode) print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    \r\n-    if (kDebugMode) print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n-    \r\n-    if (allReleases.isEmpty) {\r\n-      if (kDebugMode) print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 3. Hole alle Favoriten\r\n-    if (kDebugMode) print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n-    final favorites = await favoritesRepo.getAllFavorites();\r\n-    \r\n-    if (favorites.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n-\r\n-    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n-    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n-    if (enabledFavorites.isEmpty) {\r\n-      if (kDebugMode) print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    if (kDebugMode) print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // 4. Filtere nach Favoriten\r\n-    if (kDebugMode) print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n-    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n-    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n-      releases: todaysReleases,  // Nur heutige Releases!\r\n-      favorites: favoritesTitles,\r\n-    );\r\n-    \r\n-    if (relevantReleases.isEmpty) {\r\n-      if (kDebugMode) print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n-      return true;\r\n-    }\r\n-    \r\n-    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n-    \r\n-    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n-    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n-    \r\n-    // 6. Sende Benachrichtigungen für neue Releases\r\n-    int notificationCount = 0;\r\n-    if (kDebugMode) print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n-    \r\n-    for (final release in uniqueReleases) {\r\n-      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n-      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n-        release.title,\r\n-        release.episodeTitle,\r\n-        release.episodeNumber,\r\n-      );\r\n-      \r\n-      if (alreadyNotified) {\r\n-        if (kDebugMode) print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n-        continue;\r\n-      }\r\n-      \r\n-      // Zeige Benachrichtigung\r\n-      final notificationBody = release.isPremiere \r\n-        ? '🎬 Neue Serie: ${release.title}'\r\n-        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n-      \r\n-      await notificationService.showNotification(\r\n-        title: 'Neuer Anime Release',\r\n-        body: notificationBody,\r\n-        payload: release.seriesUrl,\r\n-      );\r\n-      \r\n-      // Logge Benachrichtigung\r\n-      final notification = NotificationLog(\r\n-        favoriteTitle: release.title,\r\n-        releaseTitle: release.episodeTitle,\r\n-        episodeNumber: release.episodeNumber,\r\n-        notifyTime: DateTime.now(),\r\n-        isShown: true,\r\n-      );\r\n-      \r\n-      await notificationRepo.logNotification(notification);\r\n-      \r\n-      notificationCount++;\r\n-      \r\n-      print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n-    }\r\n-    \r\n-    // 7. Aktualisiere lastChecked für alle Favoriten\r\n-    for (final favorite in enabledFavorites) {\r\n-      await favoritesRepo.updateLastChecked(favorite.title);\r\n-    }\r\n-    \r\n-    final duration = DateTime.now().difference(startTime);\r\n-    \r\n-    print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n-    print('   - Favorites checked: ${favorites.length}');\r\n-    print('   - Releases found: ${uniqueReleases.length}');\r\n-    print('   - Notifications sent: $notificationCount');\r\n-    \r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n-    print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n     return false;\r\n   }\r\n }\r\n-\r\n-/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n-Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Sending test notification from background...');\r\n-    \r\n-    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n-    \r\n-    // Initialisiere NotificationService\r\n-    final notificationService = NotificationService();\r\n-    await notificationService.initialize();\r\n-    \r\n-    // Sende Benachrichtigung\r\n-    await notificationService.showNotification(\r\n-      title: 'Background Test funktioniert! 🎉',\r\n-      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n-      payload: 'background_test',\r\n-    );\r\n-    \r\n-    print('✅ [BACKGROUND] Background test notification sent successfully');\r\n-    \r\n-    return true;\r\n-  } catch (e) {\r\n-    print('❌ [BACKGROUND] Test notification error: $e');\r\n-    return false;\r\n-  }\r\n-}\r\n-\r\n-/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n-Future<bool> _executeFavoritesTestNotification() async {\r\n-  try {\r\n-    print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n-    \r\n-    // Initialisiere Services\r\n-    final favoritesRepo = FavoritesRepository();\r\n-    final crunchyrollService = CrunchyrollService();\r\n-    final notificationService = NotificationService();\r\n-    \r\n-    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n-    print('📲 [BACKGROUND] Initializing notification service...');\r\n-    final initialized = await notificationService.initialize();\r\n-    if (!initialized) {\r\n-      print('❌ [BACKGROUND] Failed to initialize notification service');\r\n-      return false;\r\n-    }\r\n-    \r\n-    // Lade alle Favoriten\r\n-    print('📚 [BACKGROUND] Loading favorites from database...');\r\n-    final allFavorites = await favoritesRepo.getAllFavorites();\r\n-    print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n-    \r\n-    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n-    final enabledFavorites = \r\n-        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n-    print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n-    \r\n-    if (enabledFavorites.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Lade Releases für diese Woche\r\n-    print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n-    final now = DateTime.now();\r\n-    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n-    print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n-    \r\n-    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n-    final todayStart = DateTime(now.year, now.month, now.day);\r\n-    final todayEnd = todayStart.add(const Duration(days: 1));\r\n-    \r\n-    final todaysReleases = allReleases.where((release) {\r\n-      return release.releaseTime.isAfter(todayStart) && \r\n-             release.releaseTime.isBefore(todayEnd);\r\n-    }).toList();\r\n-    \r\n-    print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n-    \r\n-    if (todaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n-      return true;\r\n-    }\r\n-    \r\n-    // Finde Favoriten mit Releases von HEUTE\r\n-    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n-      return todaysReleases.any((release) =>\r\n-          release.title.toLowerCase() == fav.title.toLowerCase());\r\n-    }).toList();\r\n-    \r\n-    if (favoritesWithTodaysReleases.isEmpty) {\r\n-      print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n-      return true;\r\n-    }\r\n-    \r\n-    print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n-    \r\n-    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n-    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n-      final favorite = favoritesWithTodaysReleases[i];\r\n-      \r\n-      // Finde die passenden Releases für diesen Favoriten\r\n-      final matchingReleases = todaysReleases.where((release) =>\r\n-          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n-      \r\n-      for (final release in matchingReleases) {\r\n-        print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n-        \r\n-        await notificationService.showNotification(\r\n-          title: '🔔 Neue Episode: ${release.title}',\r\n-          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n-          payload: 'release_${release.title}_${release.episodeNumber}',\r\n-        );\r\n-        \r\n-        // Kleine Verzögerung zwischen Benachrichtigungen\r\n-        await Future.delayed(const Duration(milliseconds: 500));\r\n-      }\r\n-    }\r\n-    \r\n-    print('✅ [BACKGROUND] All notifications sent!');\r\n-    return true;\r\n-  } catch (e, stackTrace) {\r\n-    print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n-    print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n-    return false;\r\n-  }\r\n-}\r\n"
                },
                {
                    "date": 1768843196271,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,538 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: kDebugMode, // Nur im Debug-Modus Logs anzeigen\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  /// [delaySeconds] - Verzögerung in Sekunden bevor die Benachrichtigung gesendet wird (aus Einstellungen)\r\n+  Future<void> sendTestNotificationsForTodaysFavorites({int delaySeconds = 0}) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager (delay: ${delaySeconds}s)...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      // Mindestens 1 Sekunde, damit die App Zeit hat\r\n+      final effectiveDelay = delaySeconds > 0 ? delaySeconds : 1;\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Verwende die eingestellte Verzögerung\r\n+        initialDelay: Duration(seconds: effectiveDelay),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Favorites test task scheduled - will run in ~$effectiveDelay seconds');\r\n+        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling favorites test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n+  Future<void> testBackgroundScraperNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      \r\n+      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _taskName,  // Gleicher Task wie der periodische Scraper\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling scraper test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+      print('   - Favorites checked: ${favorites.length}');\r\n+      print('   - Releases found: ${uniqueReleases.length}');\r\n+      print('   - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+      print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Lade Releases für diese Woche\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Fetching releases for this week...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${allReleases.length} releases for this week');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        if (kDebugMode) print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+      print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768845961062,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,538 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: kDebugMode, // Nur im Debug-Modus Logs anzeigen\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  /// [delaySeconds] - Verzögerung in Sekunden bevor die Benachrichtigung gesendet wird (aus Einstellungen)\r\n+  Future<void> sendTestNotificationsForTodaysFavorites({int delaySeconds = 0}) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager (delay: ${delaySeconds}s)...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      // Mindestens 1 Sekunde, damit die App Zeit hat\r\n+      final effectiveDelay = delaySeconds > 0 ? delaySeconds : 1;\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Verwende die eingestellte Verzögerung\r\n+        initialDelay: Duration(seconds: effectiveDelay),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Favorites test task scheduled - will run in ~$effectiveDelay seconds');\r\n+        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling favorites test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n+  Future<void> testBackgroundScraperNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      \r\n+      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _taskName,  // Gleicher Task wie der periodische Scraper\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling scraper test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    if (kDebugMode) print('📤 [BACKGROUND-SCRAPER] Sending notifications...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n+      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n+        release.title,\r\n+        release.episodeTitle,\r\n+        release.episodeNumber,\r\n+      );\r\n+      \r\n+      if (alreadyNotified) {\r\n+        if (kDebugMode) print('⏭️  [BACKGROUND-SCRAPER] Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // Logge Benachrichtigung\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notification);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Notified: ${release.title} - ${release.episodeTitle}');\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+      print('   - Favorites checked: ${favorites.length}');\r\n+      print('   - Releases found: ${uniqueReleases.length}');\r\n+      print('   - Notifications sent: $notificationCount');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+      print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // WICHTIG: Verwende forceRefresh um FRISCHE Daten zu bekommen (nicht aus Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Force-fetching fresh releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.forceRefresh(forMonth: now);\r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${allReleases.length} fresh releases for this month');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        if (kDebugMode) print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+      print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1768918870855,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,562 @@\n+import 'package:workmanager/workmanager.dart';\r\n+import 'package:flutter/foundation.dart';\r\n+import '../repositories/favorites_repository.dart';\r\n+import '../repositories/notification_repository.dart';\r\n+import '../services/crunchyroll_service.dart';\r\n+import '../services/notification_service.dart';\r\n+import '../utils/release_comparator.dart';\r\n+import '../models/notification_log.dart';\r\n+\r\n+/// Service für Background-Scraping wenn die App geschlossen ist\r\n+/// Nutzt Workmanager um periodische Aufgaben zu planen\r\n+class BackgroundService {\r\n+  static const String _taskName = 'crunchyrollScraperTask';\r\n+  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n+  static const String _testNotificationTaskName = 'testNotificationTask';\r\n+  static const String _favoritesTestTaskName = 'favoritesTestNotificationTask';\r\n+  \r\n+  static final BackgroundService _instance = BackgroundService._internal();\r\n+  \r\n+  factory BackgroundService() {\r\n+    return _instance;\r\n+  }\r\n+  \r\n+  BackgroundService._internal();\r\n+  \r\n+  /// Initialisiert den Background Service\r\n+  /// Muss beim App-Start aufgerufen werden\r\n+  static Future<void> initialize() async {\r\n+    try {\r\n+      if (kDebugMode) print('🔧 Initializing Background Service...');\r\n+      \r\n+      await Workmanager().initialize(\r\n+        callbackDispatcher,\r\n+        isInDebugMode: kDebugMode, // Nur im Debug-Modus Logs anzeigen\r\n+      );\r\n+      \r\n+      if (kDebugMode) print('✓ Background Service initialized successfully');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error initializing background service: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Test-Methode: Lädt Favoriten von heute und sendet Test-Benachrichtigungen\r\n+  /// Registriert einen Background-Task für Favoriten-Test-Benachrichtigungen\r\n+  /// Dieser läuft im Hintergrund, auch wenn die App geschlossen wird!\r\n+  /// [delaySeconds] - Verzögerung in Sekunden bevor die Benachrichtigung gesendet wird (aus Einstellungen)\r\n+  Future<void> sendTestNotificationsForTodaysFavorites({int delaySeconds = 0}) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔔 [TEST] Scheduling favorites test notifications via Workmanager (delay: ${delaySeconds}s)...');\r\n+      \r\n+      final uniqueName = 'favorites_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      // Mindestens 1 Sekunde, damit die App Zeit hat\r\n+      final effectiveDelay = delaySeconds > 0 ? delaySeconds : 1;\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _favoritesTestTaskName,\r\n+        // Verwende die eingestellte Verzögerung\r\n+        initialDelay: Duration(seconds: effectiveDelay),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Favorites test task scheduled - will run in ~$effectiveDelay seconds');\r\n+        print('ℹ️  [TEST] Du kannst die App jetzt schließen, die Benachrichtigungen kommen trotzdem!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling favorites test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Test: Führt den Background-Scraper SOFORT aus (zum Testen ob er funktioniert)\r\n+  Future<void> testBackgroundScraperNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Scheduling background scraper test via Workmanager...');\r\n+      \r\n+      final uniqueName = 'scraper_test_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _taskName,  // Gleicher Task wie der periodische Scraper\r\n+        initialDelay: const Duration(seconds: 3),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [TEST] Background scraper test scheduled - will run in ~3 seconds');\r\n+        print('ℹ️  [TEST] Schließe die App und prüfe die Logs!');\r\n+      }\r\n+    } catch (e, stackTrace) {\r\n+      if (kDebugMode) {\r\n+        print('❌ [TEST] Error scheduling scraper test: $e');\r\n+        print('❌ [TEST] Stack trace: $stackTrace');\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  /// Startet die periodische Scraper-Aufgabe\r\n+  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n+  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n+    try {\r\n+      if (kDebugMode) print('📅 Attempting to register periodic scraper task...');\r\n+      \r\n+      if (intervalMinutes < 15) {\r\n+        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n+        intervalMinutes = 15;\r\n+      }\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('🔍 Registering task with:');\r\n+        print('   - Unique name: $_uniqueName');\r\n+        print('   - Task name: $_taskName');\r\n+        print('   - Interval: $intervalMinutes minutes');\r\n+        print('   - Network constraint: connected (REQUIRED)');\r\n+      }\r\n+      \r\n+      await Workmanager().registerPeriodicTask(\r\n+        _uniqueName,\r\n+        _taskName,\r\n+        frequency: Duration(minutes: intervalMinutes),\r\n+        constraints: Constraints(\r\n+          networkType: NetworkType.connected,\r\n+          requiresBatteryNotLow: false,\r\n+          requiresCharging: false,\r\n+          requiresDeviceIdle: false,\r\n+          requiresStorageNotLow: false,\r\n+        ),\r\n+        backoffPolicy: BackoffPolicy.exponential,\r\n+        backoffPolicyDelay: const Duration(minutes: 15),\r\n+        initialDelay: const Duration(minutes: 5),\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ PERIODIC TASK REGISTERED SUCCESSFULLY!');\r\n+        print('   Task will run in 5 minutes, then every $intervalMinutes minutes');\r\n+        print('   (If device has internet connection)');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ CRITICAL: Error starting periodic task: $e');\r\n+      rethrow;\r\n+    }\r\n+  }\r\n+  \r\n+  /// Stoppt die periodische Scraper-Aufgabe\r\n+  Future<void> stopPeriodicScraperTask() async {\r\n+    try {\r\n+      await Workmanager().cancelByUniqueName(_uniqueName);\r\n+      \r\n+      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n+    }\r\n+  }\r\n+  \r\n+  /// Prüft ob eine Aufgabe bereits läuft\r\n+  Future<bool> isTaskRunning() async {\r\n+    try {\r\n+      // Workmanager hat keine getAllTasks() API\r\n+      // Wir nutzen stattdessen einen einfachen Cache-Check\r\n+      // Wenn registerPeriodicTask erfolgreich war, läuft die Task\r\n+      return true; // Task läuft wenn sie registriert wurde\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error checking task status: $e');\r\n+      return false;\r\n+    }\r\n+  }\r\n+\r\n+  /// Registriert eine einmalige Test-Benachrichtigung nach X Sekunden\r\n+  /// Funktioniert auch wenn die App geschlossen ist\r\n+  Future<void> scheduleTestNotification(int delaySeconds) async {\r\n+    try {\r\n+      final uniqueName = 'test_notification_${DateTime.now().millisecondsSinceEpoch}';\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        uniqueName,\r\n+        _testNotificationTaskName,\r\n+        initialDelay: Duration(seconds: delaySeconds),\r\n+        inputData: {\r\n+          'delay_seconds': delaySeconds,\r\n+          'scheduled_time': DateTime.now().toIso8601String(),\r\n+        },\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✓ Test notification scheduled for $delaySeconds seconds from now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error scheduling test notification: $e');\r\n+    }\r\n+  }\r\n+\r\n+  /// SOFORT-TEST: Registriere einen einmaligen Task (läuft in ~1 Sekunde)\r\n+  /// Dies ist zum Testen, ob Workmanager ÜBERHAUPT funktioniert\r\n+  Future<void> testWorkmanagerNow() async {\r\n+    try {\r\n+      if (kDebugMode) print('🧪 [TEST] Registering immediate test task...');\r\n+      \r\n+      await Workmanager().registerOneOffTask(\r\n+        'workmanager_test_now',\r\n+        'workmanager_test_now',\r\n+        // Keine Verzögerung! Sollte sofort laufen\r\n+        // Keine Constraints für schnelle Tests\r\n+      );\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ Test task registered - check logs in 2-3 seconds');\r\n+        print('   Expected: 🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+        print('   Expected: 🔄 [BACKGROUND] Executing task: workmanager_test_now');\r\n+      }\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ Error registering test task: $e');\r\n+    }\r\n+  }\r\n+}\r\n+\r\n+/// Callback-Dispatcher für Background Tasks\r\n+/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n+@pragma('vm:entry-point')\r\n+void callbackDispatcher() {\r\n+  if (kDebugMode) print('🚀 [BACKGROUND] callbackDispatcher activated!');\r\n+  \r\n+  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n+    try {\r\n+      if (kDebugMode) print('🔄 [BACKGROUND] Executing task: $taskName');\r\n+      \r\n+      // Sofort-Test Task\r\n+      if (taskName == 'workmanager_test_now') {\r\n+        if (kDebugMode) print('✅ [BACKGROUND] WORKMANAGER WORKS! Immediate test successful!');\r\n+        return Future.value(true);\r\n+      }\r\n+      \r\n+      if (taskName == BackgroundService._taskName) {\r\n+        if (kDebugMode) print('📱 [BACKGROUND] Running scraper task...');\r\n+        return await _executeBackgroundScraper();\r\n+      } else if (taskName == BackgroundService._testNotificationTaskName) {\r\n+        if (kDebugMode) print('🧪 [BACKGROUND] Running test notification task...');\r\n+        return await _executeTestNotification(inputData);\r\n+      } else if (taskName == BackgroundService._favoritesTestTaskName) {\r\n+        if (kDebugMode) print('🔔 [BACKGROUND] Running favorites test notification task...');\r\n+        return await _executeFavoritesTestNotification();\r\n+      }\r\n+      \r\n+      if (kDebugMode) print('⚠️  [BACKGROUND] Unknown task: $taskName');\r\n+      return Future.value(true);\r\n+    } catch (e) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Task error: $e');\r\n+      return Future.value(false);\r\n+    }\r\n+  });\r\n+}\r\n+\r\n+/// Hauptfunktion für Background-Scraping\r\n+/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\n+Future<bool> _executeBackgroundScraper() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔄 [BACKGROUND-SCRAPER] Background scraper started at ${DateTime.now()}');\r\n+    \r\n+    final startTime = DateTime.now();\r\n+    \r\n+    // 1. Initialisiere Services\r\n+    if (kDebugMode) print('🔧 [BACKGROUND-SCRAPER] Initializing services...');\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final notificationRepo = NotificationRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND-SCRAPER] Initializing notification service...');\r\n+    final notificationInitialized = await notificationService.initialize();\r\n+    if (!notificationInitialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND-SCRAPER] Failed to initialize notification service in background');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // 2. Scrape aktuelle Releases (dies aktualisiert automatisch den Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND-SCRAPER] Fetching and caching current releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.getReleasesForWeek(now);\r\n+    \r\n+    if (kDebugMode) print('📺 [BACKGROUND-SCRAPER] Fetched and cached ${allReleases.length} releases for this week');\r\n+    \r\n+    if (allReleases.isEmpty) {\r\n+      if (kDebugMode) print('⚠️  [BACKGROUND-SCRAPER] No releases found for this week');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 3. Hole alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND-SCRAPER] Loading favorites from database...');\r\n+    final favorites = await favoritesRepo.getAllFavorites();\r\n+    \r\n+    if (favorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] No favorites configured - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Loaded ${favorites.length} total favorites');\r\n+\r\n+    // Filtere nur Favoriten mit aktivierten Benachrichtigungen\r\n+    final enabledFavorites = favorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('🔕 [BACKGROUND-SCRAPER] All favorites muted - cache updated, skipping notifications');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('🔔 [BACKGROUND-SCRAPER] Checking ${enabledFavorites.length} favorites with notifications on');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND-SCRAPER] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND-SCRAPER] Keine Releases für heute gefunden - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // 4. Filtere nach Favoriten\r\n+    if (kDebugMode) print('🔍 [BACKGROUND-SCRAPER] Filtering releases by favorites...');\r\n+    final favoritesTitles = enabledFavorites.map((f) => f.title).toList();\r\n+    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n+      releases: todaysReleases,  // Nur heutige Releases!\r\n+      favorites: favoritesTitles,\r\n+    );\r\n+    \r\n+    if (relevantReleases.isEmpty) {\r\n+      if (kDebugMode) print('✓ [BACKGROUND-SCRAPER] No new releases for favorites today - cache updated, done');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND-SCRAPER] Found ${relevantReleases.length} releases for favorites today');\r\n+    \r\n+    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n+    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n+    \r\n+    // 🚀 LOGGING UND STATISTIKEN AKTIVIEREN\r\n+    final stats = await notificationRepo.getNotificationStats(hoursBack: 2);\r\n+    if (kDebugMode) {\r\n+      print('📊 [BACKGROUND-SCRAPER] Notification Stats (last 2 hours):');\r\n+      print('   - Total sent: ${stats['totalCount']}');\r\n+      print('   - Unique content: ${stats['uniqueCount']}');\r\n+      print('   - Affected favorites: ${stats['favCount']}');\r\n+    }\r\n+    \r\n+    // 6. Sende Benachrichtigungen für neue Releases\r\n+    int notificationCount = 0;\r\n+    int skippedDuplicates = 0;\r\n+    \r\n+    if (kDebugMode) print('📤 [BACKGROUND-SCRAPER] Processing ${uniqueReleases.length} releases...');\r\n+    \r\n+    for (final release in uniqueReleases) {\r\n+      // Erstelle NotificationLog mit Content-Hash\r\n+      final notification = NotificationLog(\r\n+        favoriteTitle: release.title,\r\n+        releaseTitle: release.episodeTitle,\r\n+        episodeNumber: release.episodeNumber,\r\n+        notifyTime: DateTime.now(),\r\n+        isShown: true,\r\n+      );\r\n+      \r\n+      // 🚀 NEUE LOGIK: Prüfe auf Duplikat basierend auf Content-Hash\r\n+      final contentHash = notification.generateContentHash();\r\n+      final isDuplicate = await notificationRepo.isDuplicate(contentHash);\r\n+      \r\n+      if (isDuplicate) {\r\n+        if (kDebugMode) {\r\n+          print('⏭️  [BACKGROUND-SCRAPER] Skip duplicate: ${release.title} - ${release.episodeTitle}');\r\n+        }\r\n+        skippedDuplicates++;\r\n+        continue;\r\n+      }\r\n+      \r\n+      // Zeige Benachrichtigung\r\n+      final notificationBody = release.isPremiere \r\n+        ? '🎬 Neue Serie: ${release.title}'\r\n+        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('📤 [BACKGROUND-SCRAPER] Sending: ${release.title} - ${release.episodeTitle}');\r\n+      }\r\n+      \r\n+      await notificationService.showNotification(\r\n+        title: 'Neuer Anime Release',\r\n+        body: notificationBody,\r\n+        payload: release.seriesUrl,\r\n+      );\r\n+      \r\n+      // 🚀 Logge Benachrichtigung mit Content-Hash\r\n+      final notificationWithHash = notification.copyWith(\r\n+        contentHash: contentHash,\r\n+      );\r\n+      \r\n+      await notificationRepo.logNotification(notificationWithHash);\r\n+      \r\n+      notificationCount++;\r\n+      \r\n+      if (kDebugMode) {\r\n+        print('✅ [BACKGROUND-SCRAPER] Logged: ${release.title} - ${release.episodeTitle}');\r\n+      }\r\n+    }\r\n+    \r\n+    // 7. Aktualisiere lastChecked für alle Favoriten\r\n+    for (final favorite in enabledFavorites) {\r\n+      await favoritesRepo.updateLastChecked(favorite.title);\r\n+    }\r\n+    \r\n+    final duration = DateTime.now().difference(startTime);\r\n+    \r\n+    if (kDebugMode) {\r\n+      print('✅ [BACKGROUND-SCRAPER] Completed in ${duration.inSeconds}s');\r\n+      print('   - Favorites checked: ${favorites.length}');\r\n+      print('   - Releases found: ${uniqueReleases.length}');\r\n+      print('   - Notifications sent: $notificationCount');\r\n+      print('   - Duplicates skipped: $skippedDuplicates');\r\n+      print('   - Total to send: ${uniqueReleases.length}');\r\n+    }\r\n+    \r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND-SCRAPER] Failed: $e');\r\n+      print('❌ [BACKGROUND-SCRAPER] Stack: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Test-Benachrichtigung ausführen (im Background Isolate)\r\n+Future<bool> _executeTestNotification(Map<String, dynamic>? inputData) async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Sending test notification from background...');\r\n+    \r\n+    final delaySeconds = inputData?['delay_seconds'] ?? 0;\r\n+    \r\n+    // Initialisiere NotificationService\r\n+    final notificationService = NotificationService();\r\n+    await notificationService.initialize();\r\n+    \r\n+    // Sende Benachrichtigung\r\n+    await notificationService.showNotification(\r\n+      title: 'Background Test funktioniert! 🎉',\r\n+      body: 'Diese Benachrichtigung wurde nach $delaySeconds Sekunden im Hintergrund gesendet.',\r\n+      payload: 'background_test',\r\n+    );\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] Background test notification sent successfully');\r\n+    \r\n+    return true;\r\n+  } catch (e) {\r\n+    if (kDebugMode) print('❌ [BACKGROUND] Test notification error: $e');\r\n+    return false;\r\n+  }\r\n+}\r\n+\r\n+/// Favoriten-Test-Benachrichtigungen im Background ausführen\r\n+Future<bool> _executeFavoritesTestNotification() async {\r\n+  try {\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Starting favorites test notifications...');\r\n+    \r\n+    // Initialisiere Services\r\n+    final favoritesRepo = FavoritesRepository();\r\n+    final crunchyrollService = CrunchyrollService();\r\n+    final notificationService = NotificationService();\r\n+    \r\n+    // WICHTIG: Initialisiere NotificationService im Background Isolate!\r\n+    if (kDebugMode) print('📲 [BACKGROUND] Initializing notification service...');\r\n+    final initialized = await notificationService.initialize();\r\n+    if (!initialized) {\r\n+      if (kDebugMode) print('❌ [BACKGROUND] Failed to initialize notification service');\r\n+      return false;\r\n+    }\r\n+    \r\n+    // Lade alle Favoriten\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loading favorites from database...');\r\n+    final allFavorites = await favoritesRepo.getAllFavorites();\r\n+    if (kDebugMode) print('📚 [BACKGROUND] Loaded ${allFavorites.length} favorites');\r\n+    \r\n+    // Filtere nur Favoriten mit aktivierter Benachrichtigung\r\n+    final enabledFavorites = \r\n+        allFavorites.where((f) => f.notificationsEnabled).toList();\r\n+    if (kDebugMode) print('🔔 [BACKGROUND] Enabled favorites: ${enabledFavorites.length}');\r\n+    \r\n+    if (enabledFavorites.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit aktivierten Benachrichtigungen');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // WICHTIG: Verwende forceRefresh um FRISCHE Daten zu bekommen (nicht aus Cache!)\r\n+    if (kDebugMode) print('🌐 [BACKGROUND] Force-fetching fresh releases from Crunchyroll...');\r\n+    final now = DateTime.now();\r\n+    final allReleases = await crunchyrollService.forceRefresh(forMonth: now);\r\n+    if (kDebugMode) print('📺 [BACKGROUND] Found ${allReleases.length} fresh releases for this month');\r\n+    \r\n+    // WICHTIG: Filtere nur Releases von HEUTE (nicht aus der Vergangenheit!)\r\n+    final todayStart = DateTime(now.year, now.month, now.day);\r\n+    final todayEnd = todayStart.add(const Duration(days: 1));\r\n+    \r\n+    final todaysReleases = allReleases.where((release) {\r\n+      return release.releaseTime.isAfter(todayStart) && \r\n+             release.releaseTime.isBefore(todayEnd);\r\n+    }).toList();\r\n+    \r\n+    if (kDebugMode) print('📅 [BACKGROUND] Releases from today only: ${todaysReleases.length}');\r\n+    \r\n+    if (todaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Releases für heute gefunden');\r\n+      return true;\r\n+    }\r\n+    \r\n+    // Finde Favoriten mit Releases von HEUTE\r\n+    final favoritesWithTodaysReleases = enabledFavorites.where((fav) {\r\n+      return todaysReleases.any((release) =>\r\n+          release.title.toLowerCase() == fav.title.toLowerCase());\r\n+    }).toList();\r\n+    \r\n+    if (favoritesWithTodaysReleases.isEmpty) {\r\n+      if (kDebugMode) print('ℹ️  [BACKGROUND] Keine Favoriten mit Releases von heute');\r\n+      return true;\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('📤 [BACKGROUND] Sending notifications for ${favoritesWithTodaysReleases.length} favorites with releases today');\r\n+    \r\n+    // Sende Benachrichtigungen für jeden Favoriten mit Release heute\r\n+    for (int i = 0; i < favoritesWithTodaysReleases.length; i++) {\r\n+      final favorite = favoritesWithTodaysReleases[i];\r\n+      \r\n+      // Finde die passenden Releases für diesen Favoriten\r\n+      final matchingReleases = todaysReleases.where((release) =>\r\n+          release.title.toLowerCase() == favorite.title.toLowerCase()).toList();\r\n+      \r\n+      for (final release in matchingReleases) {\r\n+        if (kDebugMode) print('📤 [BACKGROUND] Sending notification for: ${release.title} - ${release.episodeInfo}');\r\n+        \r\n+        await notificationService.showNotification(\r\n+          title: '🔔 Neue Episode: ${release.title}',\r\n+          body: '${release.episodeInfo}: ${release.episodeTitle}',\r\n+          payload: 'release_${release.title}_${release.episodeNumber}',\r\n+        );\r\n+        \r\n+        // Kleine Verzögerung zwischen Benachrichtigungen\r\n+        await Future.delayed(const Duration(milliseconds: 500));\r\n+      }\r\n+    }\r\n+    \r\n+    if (kDebugMode) print('✅ [BACKGROUND] All notifications sent!');\r\n+    return true;\r\n+  } catch (e, stackTrace) {\r\n+    if (kDebugMode) {\r\n+      print('❌ [BACKGROUND] Favorites test notification error: $e');\r\n+      print('❌ [BACKGROUND] Stack trace: $stackTrace');\r\n+    }\r\n+    return false;\r\n+  }\r\n+}\r\n"
                }
            ],
            "date": 1768745838948,
            "name": "Commit-0",
            "content": "import 'package:workmanager/workmanager.dart';\r\nimport 'package:flutter/foundation.dart';\r\nimport '../repositories/favorites_repository.dart';\r\nimport '../repositories/notification_repository.dart';\r\nimport '../services/crunchyroll_service.dart';\r\nimport '../services/notification_service.dart';\r\nimport '../utils/release_comparator.dart';\r\nimport '../models/notification_log.dart';\r\n\r\n/// Service für Background-Scraping wenn die App geschlossen ist\r\n/// Nutzt Workmanager um periodische Aufgaben zu planen\r\nclass BackgroundService {\r\n  static const String _taskName = 'crunchyrollScraperTask';\r\n  static const String _uniqueName = 'crunchyroll-periodic-scraper';\r\n  \r\n  static final BackgroundService _instance = BackgroundService._internal();\r\n  \r\n  factory BackgroundService() {\r\n    return _instance;\r\n  }\r\n  \r\n  BackgroundService._internal();\r\n  \r\n  /// Initialisiert den Background Service\r\n  /// Muss beim App-Start aufgerufen werden\r\n  static Future<void> initialize() async {\r\n    try {\r\n      await Workmanager().initialize(\r\n        callbackDispatcher,\r\n        isInDebugMode: kDebugMode,\r\n      );\r\n      \r\n      if (kDebugMode) print('✓ Background Service initialized');\r\n    } catch (e) {\r\n      if (kDebugMode) print('❌ Error initializing background service: $e');\r\n    }\r\n  }\r\n  \r\n  /// Startet die periodische Scraper-Aufgabe\r\n  /// [intervalMinutes] - Wiederholungsintervall (minimum 15 Minuten)\r\n  Future<void> startPeriodicScraperTask({int intervalMinutes = 30}) async {\r\n    try {\r\n      if (intervalMinutes < 15) {\r\n        if (kDebugMode) print('⚠️  Minimum interval is 15 minutes, using 15');\r\n        intervalMinutes = 15;\r\n      }\r\n      \r\n      await Workmanager().registerPeriodicTask(\r\n        _uniqueName,\r\n        _taskName,\r\n        frequency: Duration(minutes: intervalMinutes),\r\n        constraints: Constraints(\r\n          networkType: NetworkType.connected,\r\n          requiresBatteryNotLow: false,\r\n          requiresCharging: false,\r\n          requiresDeviceIdle: false,\r\n          requiresStorageNotLow: false,\r\n        ),\r\n        backoffPolicy: BackoffPolicy.exponential,\r\n        backoffPolicyDelay: const Duration(minutes: 15),\r\n        initialDelay: const Duration(minutes: 5),\r\n      );\r\n      \r\n      if (kDebugMode) {\r\n        print('✓ Periodic scraper task started (interval: $intervalMinutes minutes)');\r\n      }\r\n    } catch (e) {\r\n      if (kDebugMode) print('❌ Error starting periodic task: $e');\r\n    }\r\n  }\r\n  \r\n  /// Stoppt die periodische Scraper-Aufgabe\r\n  Future<void> stopPeriodicScraperTask() async {\r\n    try {\r\n      await Workmanager().cancelByUniqueName(_uniqueName);\r\n      \r\n      if (kDebugMode) print('✓ Periodic scraper task stopped');\r\n    } catch (e) {\r\n      if (kDebugMode) print('❌ Error stopping periodic task: $e');\r\n    }\r\n  }\r\n  \r\n  /// Prüft ob eine Aufgabe bereits läuft\r\n  Future<bool> isTaskRunning() async {\r\n    try {\r\n      final allTasks = await Workmanager().getAllTasks();\r\n      return allTasks.any((task) => task.task == _taskName);\r\n    } catch (e) {\r\n      if (kDebugMode) print('❌ Error checking task status: $e');\r\n      return false;\r\n    }\r\n  }\r\n}\r\n\r\n/// Callback-Dispatcher für Background Tasks\r\n/// Diese Funktion wird in einem separaten Isolate ausgeführt\r\n@pragma('vm:entry-point')\r\nvoid callbackDispatcher() {\r\n  Workmanager().executeTask((String taskName, Map<String, dynamic>? inputData) async {\r\n    try {\r\n      if (taskName == BackgroundService._taskName) {\r\n        return await _executeBackgroundScraper();\r\n      }\r\n      \r\n      return Future.value(true);\r\n    } catch (e) {\r\n      if (kDebugMode) print('❌ Background task error: $e');\r\n      return Future.value(false);\r\n    }\r\n  });\r\n}\r\n\r\n/// Hauptfunktion für Background-Scraping\r\n/// Wird periodisch aufgerufen wenn die App geschlossen ist\r\nFuture<bool> _executeBackgroundScraper() async {\r\n  try {\r\n    if (kDebugMode) print('🔄 Background scraper started');\r\n    \r\n    final startTime = DateTime.now();\r\n    \r\n    // 1. Initialisiere Services\r\n    final favoritesRepo = FavoritesRepository();\r\n    final notificationRepo = NotificationRepository();\r\n    final crunchyrollService = CrunchyrollService();\r\n    final notificationService = NotificationService();\r\n    \r\n    // 2. Hole alle Favoriten\r\n    final favorites = await favoritesRepo.getAllFavorites();\r\n    \r\n    if (favorites.isEmpty) {\r\n      if (kDebugMode) print('ℹ️  No favorites configured');\r\n      return true;\r\n    }\r\n    \r\n    if (kDebugMode) print('📋 Checking ${favorites.length} favorites');\r\n    \r\n    // 3. Scrape aktuelle Releases\r\n    final currentReleases = await crunchyrollService.getReleasesForWeek(DateTime.now());\r\n    \r\n    if (currentReleases.isEmpty) {\r\n      if (kDebugMode) print('⚠️  No releases found');\r\n      return true;\r\n    }\r\n    \r\n    if (kDebugMode) print('📥 Found ${currentReleases.length} current releases');\r\n    \r\n    // 4. Filtere nach Favoriten\r\n    final favoritesTitles = favorites.map((f) => f.title).toList();\r\n    final relevantReleases = ReleaseComparator.filterByFavorites(\r\n      releases: currentReleases,\r\n      favorites: favoritesTitles,\r\n    );\r\n    \r\n    if (relevantReleases.isEmpty) {\r\n      if (kDebugMode) print('✓ No new releases for favorites');\r\n      return true;\r\n    }\r\n    \r\n    if (kDebugMode) print('✅ Found ${relevantReleases.length} releases for favorites');\r\n    \r\n    // 5. Prüfe auf bereits gesendete Benachrichtigungen\r\n    final uniqueReleases = ReleaseComparator.sortByRelevance(relevantReleases);\r\n    \r\n    // 6. Sende Benachrichtigungen für neue Releases\r\n    int notificationCount = 0;\r\n    \r\n    for (final release in uniqueReleases) {\r\n      // Prüfe ob Benachrichtigung bereits gesendet wurde\r\n      final alreadyNotified = await notificationRepo.hasBeenNotified(\r\n        release.title,\r\n        release.episodeTitle,\r\n        release.episodeNumber,\r\n      );\r\n      \r\n      if (alreadyNotified) {\r\n        if (kDebugMode) print('⏭️  Skip (already notified): ${release.title} - ${release.episodeTitle}');\r\n        continue;\r\n      }\r\n      \r\n      // Zeige Benachrichtigung\r\n      final notificationBody = release.isPremiere \r\n        ? '🎬 Neue Serie: ${release.title}'\r\n        : 'Folge ${release.episodeNumber}: ${release.title}';\r\n      \r\n      await notificationService.showNotification(\r\n        title: 'Neuer Anime Release',\r\n        body: notificationBody,\r\n        payload: release.seriesUrl,\r\n      );\r\n      \r\n      // Logge Benachrichtigung\r\n      final notification = NotificationLog(\r\n        favoriteTitle: release.title,\r\n        releaseTitle: release.episodeTitle,\r\n        episodeNumber: release.episodeNumber,\r\n        notifyTime: DateTime.now(),\r\n        isShown: true,\r\n      );\r\n      \r\n      await notificationRepo.logNotification(notification);\r\n      \r\n      notificationCount++;\r\n      \r\n      if (kDebugMode) print('✓ Notified: ${release.title} - ${release.episodeTitle}');\r\n    }\r\n    \r\n    // 7. Aktualisiere lastChecked für alle Favoriten\r\n    for (final favorite in favorites) {\r\n      await favoritesRepo.updateLastChecked(favorite.title);\r\n    }\r\n    \r\n    final duration = DateTime.now().difference(startTime);\r\n    \r\n    if (kDebugMode) {\r\n      print('✓ Background scraper completed in ${duration.inSeconds}s');\r\n      print('  - Favorites checked: ${favorites.length}');\r\n      print('  - Releases found: ${uniqueReleases.length}');\r\n      print('  - Notifications sent: $notificationCount');\r\n    }\r\n    \r\n    return true;\r\n  } catch (e) {\r\n    if (kDebugMode) print('❌ Background scraper failed: $e');\r\n    return false;\r\n  }\r\n}\r\n"
        }
    ]
}